#!/bin/bash
# Enforce Conventional Commits on the first line.
pattern='^(feat|fix|perf|refactor|docs|style|test|build|ci|chore)(\(.+\))?!?: .+'
if ! head -1 "$1" | grep -qE "$pattern"; then
    echo "commit message must follow Conventional Commits (e.g. 'feat: ...')" >&2
    exit 1
fi
